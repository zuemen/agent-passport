{
  "name": "agent-passport",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "sdk",
    "mcp-server",
    "demo",
    "verifier"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  }
}
